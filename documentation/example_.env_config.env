DATABASE_URI="sqlite+pysqlite:///concerts_db.sqlite"
SECRET_KEY="change-me-run-openssl-rand-hex-32"
PYTHONPATH=src

# Optional: on startup, if the events table is empty, import the most recent
# *.csv found in this directory. Use to bootstrap a dev DB from a prod export.
# DEV_SEED_CSV_DIR="/absolute/path/to/csv_directory"
